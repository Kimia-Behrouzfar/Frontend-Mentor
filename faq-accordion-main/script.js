const iconPlus = document.getElementById("icon");
const showAnswer = document.querySelector(".answer");
iconPlus.addEventListener("click", () => {
  console.log("hi");
  textContent = `<div>${showAnswer}</div>`;
  document.querySelector(".answer").textContent;
});
